{
  "$id": "saacs.auth.v0.UserCollectionRoles.jsonschema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Auth Object For RBAC",
  "patternProperties": {
    "^(collection_id)$": {
      "description": "The collection that the user is a member of",
      "minLength": 1,
      "type": "string"
    },
    "^(msp_id)$": {
      "description": "The msp of the organization that the user's certificate is from",
      "minLength": 1,
      "type": "string"
    },
    "^(role_ids)$": {
      "description": "The roles that the user has in the collection",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "^(user_id)$": {
      "description": "The id of the user from the certificate",
      "minLength": 1,
      "type": "string"
    }
  },
  "properties": {
    "collectionId": {
      "description": "The collection that the user is a member of",
      "minLength": 1,
      "type": "string"
    },
    "mspId": {
      "description": "The msp of the organization that the user's certificate is from",
      "minLength": 1,
      "type": "string"
    },
    "note": {
      "type": "string"
    },
    "roleIds": {
      "description": "The roles that the user has in the collection",
      "items": {
        "type": "string"
      },
      "type": "array"
    },
    "userId": {
      "description": "The id of the user from the certificate",
      "minLength": 1,
      "type": "string"
    }
  },
  "title": "User Collection Roles",
  "type": "object"
}
