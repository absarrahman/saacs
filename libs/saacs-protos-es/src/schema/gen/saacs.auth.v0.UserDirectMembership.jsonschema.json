{
  "$id": "saacs.auth.v0.UserDirectMembership.jsonschema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "Identity Auth Object",
  "patternProperties": {
    "^(collection_id)$": {
      "description": "The collection that the user is a member of",
      "minLength": 1,
      "type": "string"
    },
    "^(msp_id)$": {
      "description": "The msp of the organization that the user's certificate is from",
      "minLength": 1,
      "type": "string"
    },
    "^(user_id)$": {
      "description": "The id of the user from the certificate",
      "minLength": 1,
      "type": "string"
    }
  },
  "properties": {
    "collectionId": {
      "description": "The collection that the user is a member of",
      "minLength": 1,
      "type": "string"
    },
    "mspId": {
      "description": "The msp of the organization that the user's certificate is from",
      "minLength": 1,
      "type": "string"
    },
    "note": {
      "type": "string"
    },
    "polices": {
      "$ref": "saacs.auth.v0.Polices.jsonschema.json",
      "description": "The Permissions that the user will have"
    },
    "userId": {
      "description": "The id of the user from the certificate",
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "polices"
  ],
  "title": "User Direct Membership",
  "type": "object"
}
